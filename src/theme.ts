import { configureFonts, DefaultTheme, DarkTheme } from 'react-native-paper';

type FontConfiguration = {
  [key: string]: {
    [key: string]: {
      [key: string]: string;
    };
  };
};

const fontConfig = {
  web: {
    regular: {
      fontFamily: 'sans-serif',
      fontWeight: 'normal',
    },
    medium: {
      fontFamily: 'sans-serif-medium',
      fontWeight: 'normal',
    },
    light: {
      fontFamily: 'sans-serif-light',
      fontWeight: 'normal',
    },
    thin: {
      fontFamily: 'sans-serif-thin',
      fontWeight: 'normal',
    },
  },
  ios: {
    regular: {
      fontFamily: 'sans-serif',
      fontWeight: 'bold',
    },
    medium: {
      fontFamily: 'sans-serif-medium',
      fontWeight: 'normal',
    },
    light: {
      fontFamily: 'sans-serif-light',
      fontWeight: 'normal',
    },
    thin: {
      fontFamily: 'sans-serif-thin',
      fontWeight: 'normal',
    },
  },
  android: {
    regular: {
      fontFamily: 'sans-serif',
      fontWeight: 'normal',
    },
    medium: {
      fontFamily: 'sans-serif-medium',
      fontWeight: 'normal',
    },
    light: {
      fontFamily: 'sans-serif-light',
      fontWeight: 'normal',
    },
    thin: {
      fontFamily: 'sans-serif-thin',
      fontWeight: 'normal',
    },
  },
};

export const defaultTheme = {
  ...DefaultTheme,
  fonts: configureFonts(fontConfig as FontConfiguration),
  colors: {
    ...DefaultTheme.colors,
    primary: 'tomato',
    accent: 'green',
  },
};

export const darkTheme = {
  ...DarkTheme,
  fonts: configureFonts(fontConfig as FontConfiguration),
};
